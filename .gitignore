# Local .terraform dir
# Terraform dir with previously installed providers
.terraform/

# State files
*.tfstate
*.tfstate.*

# Crash logs
crash.log
crash.*.log

# Environment files - can contain passwords, keys, etc.
# Shouldn't be stored in version control!
*.tfvars
*.tfvars.json

# Override files - for local use only
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Ignore transient lock info files created by terraform apply
# Transient lock info file created by Terraform when running 'terraform apply'
.terraform.tfstate.lock.info

# Ignore CLI configuration files
# Local configuration files
.terraformrc
terraform.rc

# Graph generated by Terraform via 'terraform graph'
*.dot
